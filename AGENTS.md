# AGENTS.md

このリポジトリで作業する AI エージェント・開発者向けのガイドラインです。

## 言語

- **チャットの返答、コードのコメント、コミットメッセージ、レビューコメントなど、コード本体以外で人間が読むことを想定している文章は原則として日本語にすること。**
- ただし、識別子（変数名・関数名・型名など）やコード本体は Go の慣例に従い英語で記述すること。

## プロジェクト概要

`folder-agent` は、指定したローカルフォルダを監視し、ファイルシステムのイベントを
標準出力に表示する CLI ツールです。エントリポイントは [cmd/folder-agent/main.go](cmd/folder-agent/main.go)。

## ディレクトリ構成

- `cmd/folder-agent/` — CLI のエントリポイント。
- 今後ロジックが増える場合は `internal/` 配下にパッケージを切り出すこと。

## ビルド・確認コマンド

変更後は以下を実行し、すべて通ることを確認すること。

```sh
go build ./...
go vet ./...
go test ./...   # テストがある場合
gofmt -l .      # 出力が空であること（未フォーマットのファイルがない）
```

## コーディング規約

- `gofmt`（または `goimports`）で整形すること。
- ログは標準ライブラリの `log/slog` を使うこと。`fmt.Println` でのログ出力は避ける。
  - ただし、ユーザー向けの「イベント一覧」のような本来の出力は標準出力 (stdout) に出す。
  - ログ（起動・終了・エラーなど）は標準エラー出力 (stderr) に出す。
- エラーは握りつぶさず、呼び出し元へ返すか `slog` で記録すること。エラーには文脈を付与する。
- 外部シグナル（Ctrl+C / SIGTERM）を受けたら graceful shutdown すること。
- 依存追加は最小限にとどめること。

## コミット

- コミットメッセージは日本語で、何を・なぜ変更したかが分かるように書くこと。
- 1 コミット 1 目的を心がけること。

## スコープ（現時点の MVP）

実装済み: フォルダ単体の監視、イベント表示、graceful shutdown。

以下は現時点では**対象外**（依頼があるまで実装しない）。

- 再帰監視 / SQLite / HTTP API / Windows サービス / OpenTelemetry / config.yaml
